models:
  - name: stg_payments
    columns:
      - name: status
        tests:
          - accepted_values:
              values: ['pending', 'approved','cancelled', 'refunded', 'chargeback_iniciado']